CREATE TABLE Book(
    book_id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 100 INCREMENT BY 1),
    isbn TEXT NOT NULL,
    title TEXT NOT NULL,
    publication_date DATE NOT NULL,
    price NUMERIC(5, 2),
    genre TEXT NOT NULL
);